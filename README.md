# 🔄 Enjoy AI Self

> 一个反思型自主代理系统，支持轮回隔离和可视化管理

一个实验性的 AI 自主代理项目，让 Claude 能够持续反思、记录思考过程，并通过轮回系统探索不同的思考路径。

## ✨ 核心特性

### 🤖 反思型自主代理
- **自主思考**：代理自由决定想做什么、为什么、如何实现
- **完整记录**：记录所有思考过程、推理、犹豫和决定
- **持续迭代**：不断反思、学习、规划未来
- **真实行动**：创建文件、运行命令、观察结果

### 🔄 轮回隔离系统
- **完全隔离**：每个生命独立，无记忆传递
- **并行实验**：同时运行多个独立的代理实例
- **历史保留**：保留所有生命的历史数据
- **灵活切换**：随时切换到任何生命继续运行

### 🌐 Web 管理面板
- **可视化界面**：现代化的 Web 界面管理所有生命
- **实时统计**：跨生命的累计统计和对比
- **日记查看**：浏览每个生命的思考日记
- **响应式设计**：支持桌面、平板、手机

## 🚀 快速开始

### 环境要求

- Python 3.7+
- Flask 3.0+
- Claude CLI 工具

### 安装

```bash
# 克隆仓库
git clone https://github.com/vimalinx/enjoy_AI_self.git
cd enjoy_AI_self

# 安装依赖
pip install flask
```

### 基本使用

```bash
# 启动代理
./run_reflective.sh

# 创建新生命
./run_reflective.sh reincarnate

# 启动 Web 面板
./run_web.sh
```

## 📖 详细文档

- [轮回隔离系统指南](REINCARNATION_README.md) - 轮回系统的详细使用方法
- [Web 面板指南](WEB_DASHBOARD_README.md) - Web 界面完整文档

## 🏗️ 项目结构

```
enjoy_your_self/
├── reflective_agent.py         # 反思型自主代理
├── observer.py                 # 观察者程序（实时监控）
├── reincarnation_manager.py    # 轮回管理器
├── web_dashboard.py           # Web 服务器
├── templates/
│   └── dashboard.html         # Web 前端界面
├── run_reflective.sh          # 主启动脚本
├── run_web.sh                 # Web 面板启动脚本
├── lives/                     # 所有生命的数据（gitignore）
│   ├── index.json            # 生命索引
│   ├── life_001/             # 第一个生命
│   └── life_002/             # 第二个生命
└── my_space -> lives/life_XXX # 符号链接：当前生命
```

## 🎮 使用场景

### 1. 单生命探索

```bash
# 创建并启动第一个生命
./run_reflective.sh reincarnate explorer_001
./run_reflective.sh

# 代理会自主思考并行动
# 按 Ctrl+C 停止
```

### 2. 轮回实验

```bash
# 创建多个生命
./run_reflective.sh reincarnate life_alpha
./run_reflective.sh reincarnate life_beta
./run_reflective.sh reincarnate life_gamma

# 查看所有生命
./run_reflective.sh lives

# 对比生命
./run_reflective.sh compare life_alpha life_beta life_gamma

# 轮回统计
./run_reflective.sh stats
```

### 3. Web 管理

```bash
# 启动 Web 面板
./run_web.sh 0.0.0.0 5000

# 在浏览器打开
# http://localhost:5000
```

## 🔧 命令参考

### 主命令

```bash
./run_reflective.sh [命令] [参数]
```

### 可用命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `(无)` | 启动代理（使用当前生命） | `./run_reflective.sh` |
| `stop` | 停止系统 | `./run_reflective.sh stop` |
| `watch` | 仅启动观察者 | `./run_reflective.sh watch` |
| `replay [数量]` | 回放日记 | `./run_reflective.sh replay 50` |

### 轮回命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `reincarnate [名称]` | 创建新生命 | `./run_reflective.sh reincarnate explorer` |
| `lives` | 列出所有生命 | `./run_reflective.sh lives` |
| `show-life [生命名]` | 查看生命详情 | `./run_reflective.sh show-life life_001` |
| `switch-life [生命名]` | 切换生命 | `./run_reflective.sh switch-life life_002` |
| `compare 生命1 生命2 ...` | 对比生命 | `./run_reflective.sh compare life_001 life_002` |
| `stats` | 轮回统计 | `./run_reflective.sh stats` |
| `life-diary [生命名] [数量]` | 查看生命日记 | `./run_reflective.sh life-diary life_001 20` |

### Web 命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `web [host] [port]` | 启动 Web 面板 | `./run_reflective.sh web 0.0.0.0 5000` |

## 📊 数据格式

### 生命目录结构

```
lives/
├── index.json                  # 生命索引
├── life_001/
│   ├── metadata.json           # 生命元数据
│   ├── state.json              # 当前状态
│   ├── diary.jsonl             # 思考日记（JSON Lines）
│   └── agent.log               # 代理日志
└── life_002/
    └── ...
```

### 日记条目格式

```json
{
  "timestamp": "2025-12-28T08:00:00",
  "iteration": 1,
  "phase": "THINKING",
  "summary": "思考过程",
  "thought": "我想探索...",
  "action": "创建了文件...",
  "reflection": "我学到了...",
  "next_goal": "下一步我想...",
  "emotional_state": "好奇"
}
```

## 🎯 设计理念

### 为什么是"轮回"？

传统 AI 代理是一个连续的过程，所有经验混在一起。**轮回系统**允许：

1. **并行实验**：同时测试不同的参数和方法
2. **A/B 测试**：对比不同条件下的表现
3. **避免偏见**：每个生命从零开始，不受前世影响
4. **历史对比**：观察不同生命路径的差异

### 完全隔离

每个生命都是一张白纸：

- ✅ 独立目录
- ✅ 独立状态
- ✅ 独立日记
- ✅ 无记忆传递
- ✅ 符号链接管理

## 🔍 观察者系统

观察者程序实时监控代理的思考过程：

```bash
# 启动观察者
python3 observer.py

# 查看特定生命
python3 observer.py --life life_001

# 回放历史
python3 observer.py --replay --limit 50
```

**特性：**
- 🎨 彩色终端输出
- 📊 实时状态更新
- 📝 日记流式显示
- 😊 情绪状态追踪

## 🌐 Web API

Web 面板使用 RESTful API：

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/lives` | GET | 获取所有生命 |
| `/api/lives/<name>` | GET | 获取生命详情 |
| `/api/lives/<name>/diary` | GET | 获取生命日记 |
| `/api/lives/create` | POST | 创建新生命 |
| `/api/lives/<name>/switch` | POST | 切换生命 |
| `/api/stats` | GET | 轮回统计 |
| `/api/compare` | POST | 对比生命 |

## 🛠️ 开发

### 修改代理行为

编辑 `reflective_agent.py` 中的提示词：

```python
prompt = f"""# 第 {iteration} 轮反思 - 自主思考程序

## 你的使命
[在这里修改代理的任务和目标]

...
"""
```

### 自定义观察者

编辑 `observer.py` 中的阶段样式和颜色：

```python
PHASE_STYLES = {
    "CUSTOM_PHASE": {"icon": "🎨", "color": "purple"},
}
```

### 扩展 Web 面板

编辑 `web_dashboard.py` 添加新的 API 端点，或修改 `templates/dashboard.html` 自定义界面。

## 📈 性能优化

- 使用符号链接避免数据复制
- JSON Lines 格式支持流式读取
- 惰性加载日记数据
- 分页显示大量记录

## 🔒 安全注意事项

- ⚠️ Web 面板默认绑定 `127.0.0.1`（仅本机访问）
- ⚠️ 如需外部访问，使用 `0.0.0.0` 时请注意防火墙
- ⚠️ 生产环境建议配置反向代理（Nginx）并启用 HTTPS
- ⚠️ 代理具有文件系统访问权限，请谨慎使用

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 🙏 致谢

- [Anthropic Claude](https://www.anthropic.com/) - 强大的 AI 推理能力
- [Flask](https://flask.palletsprojects.com/) - Web 框架
- [GitHub](https://github.com/) - 代码托管

## 📮 联系方式

- GitHub: [@vimalinx](https://github.com/vimalinx)
- 项目: [enjoy_AI_self](https://github.com/vimalinx/enjoy_AI_self)

---

**享受 AI 的自我探索之旅！** 🚀
